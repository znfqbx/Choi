let Koa,Router,bodyParser,mongoose,api,createFakeData,jwtMiddleware;_1a9‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_1a9‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_1a9‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_1a9‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_1a9‍.w("./api",[["default",["api"],function(v){api=v}]]);_1a9‍.w("./createFakeData",[["default",["createFakeData"],function(v){createFakeData=v}]]);_1a9‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();
// import dotenv from 'dotenv';
// dotenv.config();









const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
    // createFakeData();
  })
  .catch((e) => {
    _1a9‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

//라우터
router.use('/api', api.routes());

//라우터 적용 전에 bodyParser
app.use(bodyParser());
app.use(jwtMiddleware);

//app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _1a9‍.g.console.log('Listening to port %d', port);
});
