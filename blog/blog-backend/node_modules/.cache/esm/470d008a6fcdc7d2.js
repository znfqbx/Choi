let Koa,Router,bodyParser,mongoose,api,createFakeData;_6fc‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_6fc‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_6fc‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_6fc‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_6fc‍.w("./api",[["default",["api"],function(v){api=v}]]);_6fc‍.w("./createFakeData",[["default",["createFakeData"],function(v){createFakeData=v}]]);require('dotenv').config();
// import dotenv from 'dotenv';
// dotenv.config();








const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
    // createFakeData();
  })
  .catch((e) => {
    _6fc‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

//라우터
router.use('/api', api.routes());

//라우터 적용 전에 bodyParser
app.use(bodyParser());

//app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _6fc‍.g.console.log('Listening to port %d', port);
});
