let dotenv,Koa,Router,bodyParser,mongoose,api;_9f2‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_9f2‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_9f2‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_9f2‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_9f2‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_9f2‍.w("./api",[["default",["api"],function(v){api=v}]]);// require('dotenv').config();

dotenv.config();







const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _9f2‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

//라우터
router.use('/api', api.routes());

//라우터 적용 전에 bodyParser
app.use(bodyParser());

//app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _9f2‍.g.console.log('Listening to port %d', port);
});
