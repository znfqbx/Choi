let Koa,Router,bodyParser,mongoose,api;_b3d‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_b3d‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_b3d‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_b3d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_b3d‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _b3d‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

//라우터
router.use('/api', api.routes());

//라우터 적용 전에 bodyParser
app.use(bodyParser());

//app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _b3d‍.g.console.log('Listening to port %d', port);
});
